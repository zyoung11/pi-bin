// Imported for side effects only: JS import semantics run this module's
// top level exactly once, in import order — reachability never makes
// module initialization lazy.
console.log("side module init");
