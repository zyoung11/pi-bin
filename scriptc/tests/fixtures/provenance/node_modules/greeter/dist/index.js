// "Built" output: what the published package ships (the source's tsc
// emit, hand-written here — the fixture pins that compiling the SOURCE
// behaves identically to island-running THIS file).
export function greet(name, options) {
  const base = `hello, ${name}`;
  return (options == null ? void 0 : options.loud) ? base.toUpperCase() + "!" : base;
}
export function exclaim(s) {
  return s + "!";
}
