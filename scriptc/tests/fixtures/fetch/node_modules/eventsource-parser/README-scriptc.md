Pinned test data: eventsource-parser 3.0.8 (MIT, see LICENSE), trimmed to
the ESM build and its types. The npm-fetch differential harness pipes a
real SSE response through EventSourceParserStream — the exact stream shape
the AI SDK consumes — under Node and under the scriptc island.
