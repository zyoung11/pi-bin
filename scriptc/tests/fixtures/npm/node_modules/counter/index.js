export class Counter {
  constructor(start) {
    this.n = start;
  }
  inc(by) {
    this.n += by;
    return this;
  }
  value() {
    return this.n;
  }
  label(prefix) {
    return `${prefix}<${this.n}>`;
  }
}
