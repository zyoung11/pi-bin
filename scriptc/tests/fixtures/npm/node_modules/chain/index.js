import adder from "adder";
import { tag } from "./inner/extra.js";
import meta from "./meta.json" with { type: "json" };
export function describe(a, b) {
  return `${tag}:${adder.add(a, b)}:${meta.label}`;
}
