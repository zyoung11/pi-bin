// The CJS export-pattern zoo — every shape Node's cjs-module-lexer
// recognizes and scriptc must lex identically at build time.
"use strict";

// The esbuild/tsup preamble: exports defined as getters through __export,
// with the dead `0 && (module.exports = {...})` annotation esbuild emits
// specifically so the lexer can see the names (as in @vercel/oidc).
var __defProp = Object.defineProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
__export(exports, {
  delta: () => delta,
  gamma: () => gamma
});
function delta() {
  return "delta()";
}
function gamma() {
  return "gamma()";
}

// Plain assignment shapes.
exports.alpha = "alpha";
module.exports.beta = "beta";

// Object.defineProperty with a value descriptor.
Object.defineProperty(exports, "epsilon", { enumerable: true, value: "epsilon" });

// A string-literal key that is not a valid identifier.
exports["dash-name"] = "dash-value";

// Lexed but never assigned: Node still creates the export — a live binding
// of undefined (the lexer sees the assignment, evaluation never runs it).
if (0) exports.phantom = "never";

// An inherited property must NOT leak into the phantom binding: Node reads
// only OWN properties of module.exports when materializing named exports.
Object.setPrototypeOf(module.exports, { phantom: "from-the-prototype" });

// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  delta,
  gamma
});
