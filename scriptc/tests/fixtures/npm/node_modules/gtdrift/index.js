// A hand-rolled __toESM whose text deviates beyond the recognizer's
// structural contract (a block body building the namespace imperatively —
// no __copyProps result, no conditional default stamp). The static
// rewrite must NOT guess at its semantics: the package degrades to the
// island with a note naming the construct, never a failed build.
"use strict";
var __toESM = (mod) => {
  var ns = { default: mod };
  for (var key in mod) ns[key] = mod[key];
  return ns;
};
var import_gtcore = __toESM(require("gtcore"));
exports.drift = function drift() {
  return import_gtcore.default.label(7) + ":" + import_gtcore.tag;
};
