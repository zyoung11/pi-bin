// A require(...) TABLE VALUE: Node's lexer adds the key, records the
// reexport, and STOPS the scan — `neverSeen` is a real runtime property
// but no named import of it links (visible through the default only).
"use strict";
module.exports = { viaTable: require("./tsc-star.js"), neverSeen: 1 };
module.exports.direct = "direct";
