// The prettier-plugins stand-in from the world-unification design's
// appendix A: an array of plugin records whose members carry engine
// functions, exported as `any` (index.d.ts) so consumers hold ISLAND
// values under --dynamic.
export const plugins = [
  {
    languages: [{ name: "js", parsers: ["babel"] }],
    options: { semi: { category: "js", type: "boolean", default: true } },
    parsers: { babel: { parse: (t) => ({ type: "root", src: t }) } },
  },
];
export const obj = { a: 1 };
export const scalars = { n: 5, s: "hi", b: true, nul: null, und: undefined };
// Engine-minted iterables (lane dom-jsval-long-tail): the PACKAGE owns
// these, so a wrapped consumer iterates the engine's own protocols —
// generator, Set, Map, and a plain array. mintGen answers a FRESH
// generator per call (one-shot protocol, engine-exact).
export const iterables = {
  list: [10, 20, 30],
  set: new Set(["s1", "s2"]),
  map: new Map([["k", "v"], ["k2", "v2"]]),
  mintGen: () => (function* () { yield 1; yield 2; })(),
  custom: { [Symbol.iterator]() { let i = 0; return { next: () => (i < 2 ? { value: "c" + i++, done: false } : { value: undefined, done: true }) } } },
  notIterable: { a: 1 },
};
