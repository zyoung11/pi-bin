// The "import" condition's entry — a real ES module (the package is
// "type": "module"). Node hands THIS file to import/import() sites and
// dist/index.cjs to require() sites: two distinct module instances, the
// documented dual-package hazard.
export const flavor = "esm";
export function parse(src) {
  return { flavor, length: String(src).length };
}
