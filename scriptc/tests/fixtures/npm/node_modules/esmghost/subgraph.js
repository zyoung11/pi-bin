// A lazily-reached ES module whose STATIC import is broken: Node links
// this subgraph at the import() boundary that reaches it — the failure
// names this file as the importer and surfaces at that call.
import "ghost-sub-pkg";
export const unreachable = true;
