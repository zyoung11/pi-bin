// The plain-CJS EXTERNAL dependency an esbuild bundle wraps in __toESM:
// no __esModule stamp, so the interop object's `default` IS this
// module.exports and every named export passes through as a getter.
"use strict";
exports.tag = "core";
exports.label = function label(n) {
  return "core#" + n;
};
