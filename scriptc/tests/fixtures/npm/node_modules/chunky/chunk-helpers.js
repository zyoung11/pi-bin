// esbuild's shared-helper chunk, byte-shaped like the vercel CLI's
// chunk-TZ2YI2VH.js: the banner creates THIS chunk's require, and
// __require closes over it — every literal __require("pkg") call site in
// OTHER chunks resolves from HERE (Node walks node_modules from this
// file; scriptc's edge attribution must land on this module's key).
import { createRequire as __createRequire } from 'node:module';
const require = __createRequire(import.meta.url);
var __require = /* @__PURE__ */ ((x) => typeof require !== "undefined" ? require : typeof Proxy !== "undefined" ? new Proxy(x, {
  get: (a, b) => (typeof require !== "undefined" ? require : a)[b]
}) : x)(function(x) {
  if (typeof require !== "undefined")
    return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});
export {
  __require
};
