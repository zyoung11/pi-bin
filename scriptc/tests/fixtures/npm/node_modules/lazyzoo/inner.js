// The deep link of the require chain: the failing require sits here, one
// module below the package entry — Node's Require stack must list THIS
// file first, then index.js (the parent chain, not the dynamic call
// stack; the probe call happens long after both modules evaluated).
"use strict";
module.exports.go = () => require("ghost-dep");
