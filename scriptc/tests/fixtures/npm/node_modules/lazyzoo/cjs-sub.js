// Lazily-reached module whose TOP-LEVEL require is broken: under Node this
// file only evaluates when someone requires it — and fails HERE, then.
"use strict";
const ghost = require("ghost-chain-dep");
module.exports = { ghost };
