// The named import Node's lexer cannot see: the checker (and the shipped
// .d.ts story) would happily bind it, but instantiate refuses the graph
// with a SyntaxError before ANY module evaluates.
import { hidden } from "lexzoo/getters.js";

export function reveal() {
  return hidden;
}
