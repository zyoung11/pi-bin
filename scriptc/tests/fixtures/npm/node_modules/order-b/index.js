// Side-effect package: its top-level MUST run at the import's position in
// Node's module-evaluation order.
console.log("order-b init");
globalThis.__order = (globalThis.__order ?? "") + "order-b;";
