// `export * as ns from` is the form ts.preProcessFile silently drops
// (zod v4 re-exports its util namespace this way) — reaching `greetings`
// through it pins the embedder's real-parse edge collection.
export * as greetings from "./greetings/en.mjs";
export const flavor = () => "peer-esm";
