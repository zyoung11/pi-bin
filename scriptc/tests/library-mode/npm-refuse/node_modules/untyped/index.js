"use strict";
// Perfectly readable JS with no declaration surface at all: the
// eligibility bar's "own .d.ts" requirement refuses it by name.
exports.rawAdd = function rawAdd(a, b) {
  return a + b;
};
