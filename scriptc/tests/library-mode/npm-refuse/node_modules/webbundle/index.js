"use strict";
// The build-transform marker the eligibility bar refuses: a webpack
// runtime shim in the shipped dist means the published JS is a bundler's
// output, not the authored source.
var __webpack_require__ = function (id) {
  return { exports: {} };
};
exports.probe = function probe() {
  return "bundled";
};
