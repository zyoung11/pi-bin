"use strict";
// A vendored fake package for the library-mode npm posture: eligible by
// every bar (own .d.ts, unminified CJS, no build-transform markers), with
// its OWN npm dep edge (mathdep) so the transitive fixpoint is exercised.
const steps = require("mathdep");

exports.scale = function scale(x, factor) {
  return steps.twice(x) * factor;
};

exports.OFFSET = 7;
