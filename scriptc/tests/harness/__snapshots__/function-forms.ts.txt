function-forms.ts:29:7 - error SC2009: values of type '(...args: number[]) => number' cannot be compiled: the function shape is supported, but its rest parameter 'args' has no compiled calling convention yet (a compiled signature is fixed-arity)

  28 | // REST signatures are never spellable as exact-arity func types.
  29 | const restRef = rest;
     |       ^~~~~~~
  30 | // Union-typed parameter defaults compile now (corpus 1535); kept here as

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

function-forms.ts:29:17 - error SC1090: functions with rest parameters as values (call them directly) are not supported yet

  28 | // REST signatures are never spellable as exact-arity func types.
  29 | const restRef = rest;
     |                 ^~~~
  30 | // Union-typed parameter defaults compile now (corpus 1535); kept here as

function-forms.ts:37:3 - error SC1090: functions with optional or defaulted parameters as values, except where the target type spells the completed signature with required parameters (a '(x?: T) => R' function flows into a '(x: T | undefined) => R' slot, and a package/'any' slot takes any signature that can cross the island boundary; otherwise call the function directly) are not supported yet

  36 | function outer(): number {
  37 |   function inner(n = 2): number {
     |   ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  38 |     return n;