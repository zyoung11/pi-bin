unassigned-fields.ts:26:3 - error SC1090: definite assignment assertions on fields not assigned at the constructor's top level ('mode!' defers the first assignment past construction — the field would hold garbage, not undefined, until it runs; assign it in the constructor or include undefined in its type) is not supported yet

  25 | class UnionLazy {
  26 |   mode!: string | null; // fenced: union-typed `!` fields have no deferral
     |   ^~~~
  27 |   init(): void {

unassigned-fields.ts:33:3 - error SC1090: definite assignment assertions on fields not assigned at the constructor's top level ('cache!' defers the first assignment past construction — the field would hold garbage, not undefined, until it runs; assign it in the constructor or include undefined in its type) is not supported yet

  32 | class MapLazy {
  33 |   cache!: Map<string, number>; // fenced: map arms cannot join the deferral union
     |   ^~~~~
  34 |   init(): void {

unassigned-fields.ts:43:1 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  42 | new Eager();
  43 | new UnionLazy();
     | ^~~~~~~~~~~~~~~
  44 | new MapLazy();

unassigned-fields.ts:44:1 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  43 | new UnionLazy();
  44 | new MapLazy();
     | ^~~~~~~~~~~~~
  45 | 