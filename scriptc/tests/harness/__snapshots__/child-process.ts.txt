child-process.ts:10:16 - error SC2020: 'spawn without { stdio: "ignore" }' is part of the standard library types but has no scriptc lowering yet

   9 | // options never silently loses the child's output.
  10 | const noOpts = spawn("/bin/echo");
     |                ^~~~~~~~~~~~~~~~~~
  11 | 

  hint: Node's default stdio is "pipe" (streams, no lowering) — pass { stdio: "ignore" } or { stdio: "inherit" } explicitly, or capture with spawnSync

child-process.ts:14:47 - error SC2020: 'spawn with stdio: "pipe"' is part of the standard library types but has no scriptc lowering yet

  13 | // lowering — each names its gap.
  14 | const piped = spawn("/bin/echo", [], { stdio: "pipe" });
     |                                               ^~~~~~
  15 | const inherited = spawn("/bin/echo", [], { stdio: "inherit" });

  hint: piped STDIN has no lowering — pipe stdout/stderr with the tuple form (stdio: ["ignore", "pipe", "pipe"]), or capture with spawnSync

child-process.ts:23:14 - error SC1090: listeners returning a value (make the callback body a block, or return nothing) is not supported yet

  22 | // as void.
  23 | c.on("exit", (): number => 5);
     |              ^~~~~~~~~~~~~~~
  24 | 

child-process.ts:26:7 - error SC2007: values of type '{ (event: "exit", listener: (code: number | null, signal: string | null) => void): void; (event: "error", listener: (err: Error) => void): void; }' cannot be compiled: the type declares multiple call signatures (overloads), and a compiled function value is always one concrete signature

  25 | // Methods have no bound-value form — call on directly.
  26 | const f = c.on;
     |       ^
  27 | 

  hint: annotate the slot with the ONE signature the program actually calls (e.g. '(x: number) => string'), or wrap the overloaded function in a single-signature arrow

child-process.ts:26:11 - error SC1090: child methods as values (call 'on' directly) are not supported yet

  25 | // Methods have no bound-value form — call on directly.
  26 | const f = c.on;
     |           ^~~~
  27 | 