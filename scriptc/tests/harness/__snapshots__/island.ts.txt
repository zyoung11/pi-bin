island.ts:4:11 - error SC2010: '__island_eval' requires the embedded dynamic engine, which this build does not include

  3 | // ICE and never a C-level link error.
  4 | const r = __island_eval("6 * 7");
    |           ^~~~~~~~~~~~~~~~~~~~~~
  5 | console.log(r);

  hint: build with --dynamic to embed the engine (adds ~620KB to the binary); static builds never include it

island.ts:8:10 - error SC2010: '__island_eval' requires the embedded dynamic engine, which this build does not include

  7 | function indirect(code: string): string {
  8 |   return __island_eval(code);
    |          ^~~~~~~~~~~~~~~~~~~
  9 | }

  hint: build with --dynamic to embed the engine (adds ~620KB to the binary); static builds never include it