intl.ts:10:18 - error SC2020: 'Intl.NumberFormat without a locale' is part of the standard library types but has no scriptc lowering yet

   9 | // The unlowered NumberFormat variants, each with its own reason.
  10 | const noLocale = new Intl.NumberFormat().format(1234.5);
     |                  ^~~~~~~~~~~~~~~~~~~~~~~
  11 | const otherLocale = new Intl.NumberFormat("de-DE").format(1234.5);

  hint: the default locale is the host environment's, which a compiled binary cannot carry — pass it explicitly: new Intl.NumberFormat("en-US").format(x)

intl.ts:11:43 - error SC2020: 'Intl.NumberFormat at locale "de-DE"' is part of the standard library types but has no scriptc lowering yet

  10 | const noLocale = new Intl.NumberFormat().format(1234.5);
  11 | const otherLocale = new Intl.NumberFormat("de-DE").format(1234.5);
     |                                           ^~~~~~~
  12 | const withOptions = new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 }).format(1234.5);

  hint: "en-US" (Node's default-build locale) is the one locale whose data the runtime embeds — everything else is ICU data the binary does not carry

intl.ts:12:52 - error SC2020: 'Intl.NumberFormat with an options bag' is part of the standard library types but has no scriptc lowering yet

  11 | const otherLocale = new Intl.NumberFormat("de-DE").format(1234.5);
  12 | const withOptions = new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 }).format(1234.5);
     |                                                    ^~~~~~~~~~~~~~~~~~~~~~~~~~~~
  13 | const hoisted = new Intl.NumberFormat("en-US");

  hint: the embedded data covers DEFAULT options only (decimal notation, up to 3 fraction digits, grouping) — new Intl.NumberFormat("en-US").format(x)

intl.ts:13:7 - error SC2020: 'NumberFormat' is part of the standard library types but has no scriptc lowering yet

  12 | const withOptions = new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 }).format(1234.5);
  13 | const hoisted = new Intl.NumberFormat("en-US");
     |       ^~~~~~~
  14 | const viaValue = hoisted.format(1234.5);

  hint: Intl formatter values have no representation — the COMPOSED en-US forms lower: new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; the rest is ICU locale data the binary does not carry

intl.ts:13:17 - error SC1090: constructing values other than classes declared in the program is not supported yet

  12 | const withOptions = new Intl.NumberFormat("en-US", { maximumFractionDigits: 2 }).format(1234.5);
  13 | const hoisted = new Intl.NumberFormat("en-US");
     |                 ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  14 | const viaValue = hoisted.format(1234.5);

intl.ts:14:18 - error SC2020: 'NumberFormat.format' is part of the standard library types but has no scriptc lowering yet

  13 | const hoisted = new Intl.NumberFormat("en-US");
  14 | const viaValue = hoisted.format(1234.5);
     |                  ^~~~~~~~~~~~~~
  15 | 

  hint: locale- and ICU-backed behavior lives outside the static runtime (the localeCompare stance: code-unit order, no collation/locale data) — what lowers: the composed new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; format with template literals, toFixed, and toString otherwise

intl.ts:17:20 - error SC2020: 'Number.prototype.toLocaleString without a locale' is part of the standard library types but has no scriptc lowering yet

  16 | // toLocaleString's unlowered variants.
  17 | const envDefault = (1234.5).toLocaleString();
     |                    ^~~~~~~~~~~~~~~~~~~~~~~~~
  18 | const german = (1234.5).toLocaleString("de-DE");

  hint: the default locale is the host environment's, which a compiled binary cannot carry — pass it explicitly: x.toLocaleString("en-US")

intl.ts:18:40 - error SC2020: 'Number.prototype.toLocaleString at locale "de-DE"' is part of the standard library types but has no scriptc lowering yet

  17 | const envDefault = (1234.5).toLocaleString();
  18 | const german = (1234.5).toLocaleString("de-DE");
     |                                        ^~~~~~~
  19 | const grouped = (1234.5).toLocaleString("en-US", { useGrouping: false });

  hint: "en-US" (Node's default-build locale) is the one locale whose data the runtime embeds — everything else is ICU data the binary does not carry

intl.ts:19:17 - error SC2020: 'Number.prototype.toLocaleString with an options bag' is part of the standard library types but has no scriptc lowering yet

  18 | const german = (1234.5).toLocaleString("de-DE");
  19 | const grouped = (1234.5).toLocaleString("en-US", { useGrouping: false });
     |                 ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  20 | 

  hint: the embedded data covers DEFAULT options only (decimal notation, up to 3 fraction digits, grouping) — x.toLocaleString("en-US")

intl.ts:22:14 - error SC2020: 'DateTimeFormat.format' is part of the standard library types but has no scriptc lowering yet

  21 | // The rest of Intl keeps the ICU story.
  22 | const when = new Intl.DateTimeFormat("en-US").format();
     |              ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  23 | const dur = new Intl.DurationFormat("en-US").format({ hours: 1, minutes: 23 });

  hint: locale- and ICU-backed behavior lives outside the static runtime (the localeCompare stance: code-unit order, no collation/locale data) — what lowers: the composed new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; format with template literals, toFixed, and toString otherwise

intl.ts:23:13 - error SC2020: 'DurationFormat.format' is part of the standard library types but has no scriptc lowering yet

  22 | const when = new Intl.DateTimeFormat("en-US").format();
  23 | const dur = new Intl.DurationFormat("en-US").format({ hours: 1, minutes: 23 });
     |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  24 | const rule = new Intl.PluralRules("en-US").select(1);

  hint: locale- and ICU-backed behavior lives outside the static runtime (the localeCompare stance: code-unit order, no collation/locale data) — what lowers: the composed new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; format with template literals, toFixed, and toString otherwise

intl.ts:24:14 - error SC2020: 'PluralRules.select' is part of the standard library types but has no scriptc lowering yet

  23 | const dur = new Intl.DurationFormat("en-US").format({ hours: 1, minutes: 23 });
  24 | const rule = new Intl.PluralRules("en-US").select(1);
     |              ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  25 | const canon = Intl.getCanonicalLocales("en-us");

  hint: locale- and ICU-backed behavior lives outside the static runtime (the localeCompare stance: code-unit order, no collation/locale data) — what lowers: the composed new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; format with template literals, toFixed, and toString otherwise

intl.ts:25:15 - error SC2020: 'Intl.getCanonicalLocales' is part of the standard library types but has no scriptc lowering yet

  24 | const rule = new Intl.PluralRules("en-US").select(1);
  25 | const canon = Intl.getCanonicalLocales("en-us");
     |               ^~~~~~~~~~~~~~~~~~~~~~~~
  26 | 

  hint: locale- and ICU-backed behavior lives outside the static runtime (the localeCompare stance: code-unit order, no collation/locale data) — what lowers: the composed new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; format with template literals, toFixed, and toString otherwise