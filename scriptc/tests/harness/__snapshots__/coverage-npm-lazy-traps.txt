scriptc coverage tests/fixtures/npm/cases/lazy-traps/main.ts

  statements analyzed   23
  compile statically    12  (52%)
  compile dynamically   11  (47%) (island sites — the embedded engine runs them)

  embedded npm code imports Node builtins and unresolved specifiers:
    ./missing-rel.js    unresolvable — lazy trap  (import() in esmghost)
    ./nope              unresolvable — lazy trap  (require in lazyzoo)
    ghost-bare-pkg      unresolvable — lazy trap  (import() in esmghost)
    ghost-chain-dep     unresolvable — lazy trap  (require in lazyzoo)
    ghost-dep           unresolvable — lazy trap  (require in lazyzoo)
    ghost-sub-pkg       unresolvable — lazy trap  (import in esmghost)
    left-padz           unresolvable — lazy trap  (require in lazyzoo)
    never-imported-pkg  unresolvable — lazy trap  (import() in esmghost)
    never-there         unresolvable — lazy trap  (require in lazyzoo)
    (lazy trap: only reachable through require()/import() boundaries — the build embeds Node's call-time error; the call throws at runtime)

  builds with --dynamic — no remaining blockers (the island sites above run in the embedded engine).