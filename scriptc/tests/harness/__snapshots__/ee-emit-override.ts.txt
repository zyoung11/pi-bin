ee-emit-override.ts:9:3 - error SC1090: overriding EventEmitter's 'emit' outside the forwarding shape (the rest parameter may only forward through super.emit(event, ...args) in the method's own body; the compiled form is `emit(event: string, ...args: unknown[]): boolean`) is not supported yet

   8 | class Peeker extends EventEmitter {
   9 |   emit(event: string, ...args: unknown[]): boolean {
     |   ^~~~
  10 |     console.log(args.length); // reads the rest parameter — not a forward

ee-emit-override.ts:16:3 - error SC1090: overriding the EventEmitter member 'on' (the runtime owns the emitter surface) is not supported yet

  15 | class Wrapped extends EventEmitter {
  16 |   on(event: string, listener: (...a: unknown[]) => void): this {
     |   ^~
  17 |     return super.on(event, listener);

ee-emit-override.ts:28:1 - error SC2020: ''on' of 'newListener' while 'Fine' overrides emit' is part of the standard library types but has no scriptc lowering yet

  27 | const f = new Fine();
  28 | f.on("newListener", (name: string) => console.log("meta", name));
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  29 | f.emit("x");

  hint: the runtime fires meta events internally, which cannot route through an emit override

ee-emit-override.ts:30:13 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  29 | f.emit("x");
  30 | console.log(new Peeker());
     |             ^~~~~~~~~~~~
  31 | console.log(new Wrapped());

ee-emit-override.ts:31:13 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  30 | console.log(new Peeker());
  31 | console.log(new Wrapped());
     |             ^~~~~~~~~~~~~
  32 | 