object-create-fences.ts:10:19 - error SC2020: 'Object.create over '{ indent: number }' prototypes' is part of the standard library types but has no scriptc lowering yet

   9 | const base = { indent: 2 };
  10 | const viaRecord = Object.create(base);
     |                   ^~~~~~~~~~~~~~~~~~~
  11 | 

  hint: the compiled representations have no prototype chain, and an own-copy would answer wrong observably (Node lists NO own keys on the created object, and prototype mutations show through it live) — only Object.create(null) lowers

object-create-fences.ts:14:16 - error SC2020: 'Object.create over 'unknown' prototypes' is part of the standard library types but has no scriptc lowering yet

  13 | const dynProto: object = JSON.parse('{"a":1}');
  14 | const viaDyn = Object.create(dynProto);
     |                ^~~~~~~~~~~~~~~~~~~~~~~
  15 | 

  hint: the compiled representations have no prototype chain, and an own-copy would answer wrong observably (Node lists NO own keys on the created object, and prototype mutations show through it live) — only Object.create(null) lowers

object-create-fences.ts:17:25 - error SC2020: 'Object.create with a properties-descriptor argument' is part of the standard library types but has no scriptc lowering yet

  16 | // The properties-descriptor form.
  17 | const withDescriptors = Object.create(null, { a: { value: 1, enumerable: true } });
     |                         ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  18 | 

  hint: create first, then assign: const o = Object.create(null); o.k = v