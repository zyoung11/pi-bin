dynamic-representable-types.ts:9:7 - error SC2011: values of type 'any[]' have no static representation but run in the embedded dynamic engine, which this build does not include

   8 |  * (an optional-element tuple) keep the SC2001 recitation. */
   9 | const arr: any[] = [1, "x"];
     |       ^~~
  10 | var evolving = []; // never used — the tsc-clean evolving-array shape

  hint: build with --dynamic to run these values in the embedded engine (adds ~620KB to the binary), or restate the type inside the static surface (https://scriptc.dev/limitations describes the boundary)

dynamic-representable-types.ts:10:5 - error SC2011: values of type 'any[]' have no static representation but run in the embedded dynamic engine, which this build does not include

   9 | const arr: any[] = [1, "x"];
  10 | var evolving = []; // never used — the tsc-clean evolving-array shape
     |     ^~~~~~~~
  11 | const rec = { workItem: {} as any, width: "10px" };

  hint: build with --dynamic to run these values in the embedded engine (adds ~620KB to the binary), or restate the type inside the static surface (https://scriptc.dev/limitations describes the boundary)

dynamic-representable-types.ts:11:7 - error SC2011: values of type '{ workItem: any; width: string; }' have no static representation but run in the embedded dynamic engine, which this build does not include

  10 | var evolving = []; // never used — the tsc-clean evolving-array shape
  11 | const rec = { workItem: {} as any, width: "10px" };
     |       ^~~
  12 | const fn: (v: any) => string = (v) => String(v);

  hint: build with --dynamic to run these values in the embedded engine (adds ~620KB to the binary), or restate the type inside the static surface (https://scriptc.dev/limitations describes the boundary)

dynamic-representable-types.ts:11:13 - error SC2011: values of type '{ workItem: any; width: string; }' have no static representation but run in the embedded dynamic engine, which this build does not include

  10 | var evolving = []; // never used — the tsc-clean evolving-array shape
  11 | const rec = { workItem: {} as any, width: "10px" };
     |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  12 | const fn: (v: any) => string = (v) => String(v);

  hint: build with --dynamic to run these values in the embedded engine (adds ~620KB to the binary), or restate the type inside the static surface (https://scriptc.dev/limitations describes the boundary)

dynamic-representable-types.ts:14:7 - error SC2011: values of type '[number, (string | undefined)?]' have no static representation but run in the embedded dynamic engine, which this build does not include

  13 | const bare: any = 41;
  14 | const optTuple: [number, string?] = [1];
     |       ^~~~~~~~
  15 | console.log(arr.length, rec.width, fn(1), bare, optTuple.length);

  hint: build with --dynamic to run these values in the embedded engine (adds ~620KB to the binary), or restate the type inside the static surface (https://scriptc.dev/limitations describes the boundary)

dynamic-representable-types.ts:15:25 - error SC2004: uses of 'rec' inherit the blocker on its declaration

  14 | const optTuple: [number, string?] = [1];
  15 | console.log(arr.length, rec.width, fn(1), bare, optTuple.length);
     |                         ^~~~~~~~~
  16 | export const marker: number = 1;

  hint: the declaration of 'rec' did not compile — fix the diagnostic reported there and these sites clear with it